% clause database

% procedure - isInteger/1
isInteger(0).   % fact
isInteger(1).   % fact
isInteger(2).   % fact
isInteger(car). % fact

% isInteger/2
isInteger(3, 4).

% equals/2
equals(X, X).
equals(1, 2).

